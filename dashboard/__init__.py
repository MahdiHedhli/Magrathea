"""Magrathea dashboard — a read-only, localhost-bound projection of existing state.

The one rule (enforced by dashboard/tests/, not convention): read-only, no control
actions, no model calls, localhost only. See specs/002-dashboard/spec.md.
"""

__version__ = "1.0.0"
